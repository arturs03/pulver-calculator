export interface IPaintAreaData {
  area: number
  dimmensions: object
  figureType: string
}

export interface IOverlayType {
  selectedOverlay: string
  selectedOverlayLook: string
}

export interface ITransportation {
  selectedTransportation: string
  comments: string
}

export interface ISection {
  title: string
  step: number
}
